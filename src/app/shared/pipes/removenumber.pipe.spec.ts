import { RemovenumberPipe } from './removenumber.pipe';

describe('RemovenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovenumberPipe();
    expect(pipe).toBeTruthy();
  });
});
