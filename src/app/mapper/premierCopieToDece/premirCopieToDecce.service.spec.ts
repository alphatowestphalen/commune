/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PremirCopieToDecceService } from './premirCopieToDecce.service';

describe('Service: PremirCopieToDecce', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PremirCopieToDecceService]
    });
  });

  it('should ...', inject([PremirCopieToDecceService], (service: PremirCopieToDecceService) => {
    expect(service).toBeTruthy();
  }));
});
